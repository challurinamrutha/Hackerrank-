#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
int X;
int app[1000];
int main()
{
long int sum=0;
scanf("%d",&X);
for(int i=0;i<X;i++){
    scanf("%d",&app[i]);
}
for(int i=0;i<X;i++)
{
    sum += app[i];
}
printf("%ld",sum);
return 0;
}
